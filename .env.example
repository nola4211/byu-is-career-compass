# Public project connection URL. This is not a credential.
LIVEKIT_URL=wss://is-core-case-2026-zat9gox0.livekit.cloud

# Server-only credentials. Add real values to the hosting environment; never
# commit them or expose them through a NEXT_PUBLIC_ variable.
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# Fixed deployed agent name and the production site origin allowed to request
# interview tokens.
LIVEKIT_AGENT_NAME=career-interviewer
LIVEKIT_ALLOWED_ORIGIN=http://localhost:3000
